# RAG

Reproducible code for a Retrieval-Augmented Generation (RAG) pipeline driven by YAML configs.

- **Code:** this GitHub repo (folders: `src/`, `configs/`). :contentReference[oaicite:0]{index=0}  
- **Data:** hosted on **Zenodo** (download separately; see **Dataset** section below).

---

## Repository layout

```text
RAG/
├─ src/          # main scripts (training / retrieval / generation)
├─ configs/      # YAML experiment configs
└─ README.md
